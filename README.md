# stras-go

A minimal Go board for playing over-the-table, in the browser. There is no
server; everything is stored in the URL. Copy the link at any point and open it
anywhere to pick up exactly where you left off.

## Development

```bash
npm test          # rules engine and codec tests (node --test)
npm run serve     # http://localhost:8080
```

