{
  "name": "stras-go",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal over-the-table Go in the browser. The URL is the save file.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "python3 -m http.server 8080"
  }
}
